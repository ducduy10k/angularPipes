import { TopRecordPipe } from './top-record.pipe';

describe('TopRecordPipe', () => {
  it('create an instance', () => {
    const pipe = new TopRecordPipe();
    expect(pipe).toBeTruthy();
  });
});
